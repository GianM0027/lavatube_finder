papers
.venv
.idea
__pycache__
.env
lavatube_finder.ipynb
to-do
trade_off.py
model/best_model*.pt
matched_crops_preview.png
# Datasets are ignored, but the generation code that lives beside them is not.
data/**
!data/**/
!data/**/*.py
!data/**/*.ipynb
# Small reference catalogues are kept: they are inputs, not generated,
# and the notebooks cannot run without them.
!data/*.csv
model/cv_cache/
model/checkpoints/
